# wrapped_token_registry.aleo

**Stateful** wrapper around `token_registry.aleo` implementing the `IARC20`. Provides an ARC20-compliant adapter for a specific `token_registry` token ID.

## Dependency

`token_registry.aleo` (local)
